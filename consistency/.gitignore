# A patched copy of the SDK's mathlib.h, generated by build-win.sh. Derived from
# Valve's header, so it is not ours to redistribute, and it is regenerated on
# every build anyway.
build/shim/
build/*.obj
# import library lld-link emits beside the DLL; nothing consumes it
build/*.lib
build/*.exp
